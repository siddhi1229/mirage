{
  "name": "sentinel-blockchain",
  "version": "1.0.0",
  "description": "ThreatChain smart contract for Sentinel MLaaS Security Platform",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy:hardhat": "hardhat run scripts/deploy.js --network hardhat",
    "node": "hardhat node",
    "verify": "hardhat run scripts/verify.js --network localhost",
    "service": "node blockchain-service/server.js",
    "start": "npm run service"
  },
  "keywords": [
    "blockchain",
    "smart-contract",
    "threat-detection",
    "security",
    "hardhat"
  ],
  "author": "Sentinel Team",
  "license": "MIT",
  "devDependencies": {
    "hardhat": "^2.22.4",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "ethers": "^6.7.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}